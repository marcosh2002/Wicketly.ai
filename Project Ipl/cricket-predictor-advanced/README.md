# 🏏 Cricket Predictor

Full-stack cricket prediction website using ML (XGBoost), Flask backend, and HTML frontend.

## 🚀 Quick Start

```bash
# Backend
cd backend
pip install -r requirements.txt
python scraper.py
python model.py
python app.py
```

Open `frontend/index.html` in browser.
