{
  "$schema": "https://railway.app/json-schema/railwayapp.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "docker build -t app .",
    "dockerfile": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "python backend/app.py",
    "restartPolicyMaxRetries": 5
  }
}
